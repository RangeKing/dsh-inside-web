# DSH Inside Web 开发计划

设计确认：2026-09-18，grilling Q1–Q15 全部形成共识。用户补充优先：创造模式改为像素 AI 公司；网页主角称“大肥鲸”；整体幽默有趣；旧案例必须核验和优化。

## 交付范围

独立的静态科普网站。首页约 3–5 分钟阅读，桌面完整 3D，手机内容完整、特效降级；双语与深浅主题。顶部可直达案例，底部主入口进入案例库。案例地址可分享，返回首页保留阅读位置。原有模式比较、接口图鉴、资料查看和本地记录导入保留并纠正旧语义。

本次不连接真实 DSH、不运行真实模型、不公开部署。图像生成可用于公司讲解素材；素材进入项目并标注来源，文字和交互由 HTML 绘制。

## 首页分镜

| 章节 | 讲解 | 模型与交互 |
|---|---|---|
| 01 认识大肥鲸 | 模型与 Harness 的分工 | 整艇入场；开始下潜、直达案例 |
| 02 打开外壳 | 一切皆插件；可组合和替换 | 外壳展开，模块显露 |
| 03 接到任务 | 输入、请求、工具、反馈与循环 | Agent Loop、LLM、Session 信号 |
| 04 接上能力 | Definition / Provider / Consumer；工具检查与审批 | Seam、工具舱、气闸 |
| 05 记住发生过什么 | 会话记录、上下文投影与压缩 | Session Spine、Compaction |
| 06 分工与扩展 | 子 Agent、后台任务、预设、插件生效 | 子艇、Job、工坊；有条件能力明确标注 |
| 07 一起开工 | 组件如何服务一项任务 | 整艇协作；进入八个案例 |

2026-09-18 用户追加视觉要求：参考苹果官网产品介绍的视差滚动，重做首页质感。以近黑/浅白背景、居中产品首屏、大字短句、长距离 sticky 分镜、模型推近/展开/聚焦/重组构成叙事；中间章节交替构图，详细解释后置，移除坐标、装饰环和仪表盘感。参考 https://www.apple.com/macbook-pro/ 的构图与阅读节奏，不复用苹果图像或品牌。黑白大肥鲸辅以克制的冷色轮廓光。镜头随正常滚动前后推进，不劫持滚轮。点击模块显示浅层解释，可展开源码。探索模式才接管旋转/缩放。减少动态效果时保留章节对应的静态场景。

## 案例迁移

| 案例 | 保留目标 | 新版核验重点 |
|---|---|---|
| 初识 DSH | 任务循环 | Turn / Step / LLM / Tool 的顺序和边界 |
| 聚餐分账 | 工具组织与预设比较 | 四预设真实组成，移除旧 Cordis 临时工具生命周期 |
| 整理照片 | 拒绝发生在执行之前 | 配置策略、审批、取消与拒绝，避免把提示词当强制策略 |
| 共享采购单 | 并发更新与重新读取 | 当前文件观察/写入约束、错误码、重试依据 |
| 报名表合并 | 文件处理与程序化工具调用 | PTC 工具与结果处理；资料计算可重现 |
| 迎新活动 | 子 Agent 分工 | 当前委派方式、父子关系、能力继承与结果汇总 |
| 航行档案 | 长上下文与证据保留 | 当前 compaction；示意压力非测量，保留两种摘要分支 |
| 像素 AI 公司 | Creator 扩展 DSH | 运行时检查→插件制作/安装→确认生效→协作→停用/移除 |

公司第一单：“给大肥鲸做一个能用的番茄钟网页”。教学角色：接单协调、设计、开发、测试；过程包含发现问题与修复。办公室是插件的示例界面，角色不是未经证明的真实运行者。教学产物提供可交互的番茄钟示例。安装和成功调用分开；插件生命周期与订单执行分开。

## 开发阶段与完成条件

- [x] P0：设计确认；建立 AGENTS.md 和本计划。
- [x] P1：保存旧版；明确 v4 与 src 差异；核验最新上游，形成按案例的来源表与漂移记录；盘点资产与许可。
- [x] P2：统一静态构建入口；共享 GLB 注册表/加载器；验证主艇与模块加载，完成首页七章、章节跳转、探索模式和降级。
- [x] P3：八个案例内容核验；共享 3D 场景、时间线、点击详情；保留二维/接口详情、模式比较、资料与日志阅读。
- [x] P4：像素办公室素材和状态界面、插件安装教学、番茄钟产物；补齐双语与主题。
- [x] P5：构建、内容一致性与关键行为检查；实际浏览器检查桌面与手机、资源加载与页面切换；记录最终证据与限制。

P0 完成后按本计划连续开发。常规技术选择无需追加确认；新增产品范围或公开发布另议。

## 验收矩阵

1. 无 hash 首次进入科普首页；顶部与底部均能进入案例库；案例深链接刷新可恢复；回到首页保留章节/位置。
2. 七章可以正向、反向、直接跳转；无滚轮劫持；探索模式可以明确退出。关键场景可复现，不靠历史动画累积。
3. 主艇与功能模块来自已记录的 dsh-inside 资产；GLB 真正加载，无假模型替代成功声明；可见中文不出现“虎鲸”。
4. 八案例均有当前来源、资料/目标/步骤/结果；过时流程已改或明确删除依据；不混用旧提交与新版本声明。
5. 3D 高亮、选择、二维关系和文字详情来自同一状态；快速 seek、自动播放、切换案例不串状态。
6. 公司教学包含插件生效条件、岗位任务状态、返工与交付；示例图明确为生成的教学素材；番茄钟能开始、暂停和复位。
7. 中英文切换、深浅主题、键盘焦点、窄屏布局可用；移动端不横向溢出；reduced-motion 下无需观看动画才能阅读。
8. 模型加载失败/WebGL 不可用仍能完成阅读和案例操作；卸载时释放资源；无持续后台 RAF 泄漏。
9. 日志导入留在本地内存；格式/大小限制、未知事件与缺失因果维持明确边界；旧原始存储格式不冒充新版全面兼容。
10. 本地检查与浏览器实际检查分别记录；未运行真实 DSH、未公开部署、资产分发权未确认等限制如实保留。

## 初始核验记录

- 旧网站 data/catalog.json：0.1.3-alpha.1，d347e703908d0406b7a7ef80e3a0e594d86b2215。
- 讨论时 GitHub 最新标签/HEAD：dsh-v0.1.6-alpha.2，ddefc45fbc7f8e46dd73185e68295696d1297887。开发时重新确认。
- 最新 tool-cordis 文档：只读 runtime inspection；Creator 的持久修改由 Plugin Manager 负责。旧 cordis_define/run 流程不能继续作为现行机制。
- 当前无 Git 仓库；index.html、v4 独立页与 src 有差异。实现前保留快照，采用可恢复迁移。
- 参考项目 production 主艇约 11 MB，配有 LOD；资产包说明与实际产物可能不同，以实际 metadata/GLB 为准。

## 实际交付状态

P1–P5 已完成本地实现与分层验证。原版位于 `archive/pre-redesign-20260918.tar.gz`；新版为多资源静态构建。用户追加的苹果产品介绍页方向已落到首页结构、样式、光照和确定性滚动镜头中。详细证据、实际覆盖与未覆盖边界见 `docs/QA.md`。

- 构建成功；初次交付 8 项自动检查通过；动画补强后 12 项通过。
- 已检查桌面/手机、七章首页、八案例、四条分账预设路线、两种摘要分支、番茄钟、日志与无图形降级。
- AGENTS、开发计划、来源核验、资产清单、第三方声明及 QA 记录已同步。
- 真实 DSH 执行、公开上线、跨浏览器与实体手机全矩阵不属于已完成声明。

## 2026-09-19 动画与科普补强

用户指出滚动不流畅、3D 缺少标注、插件接入和执行循环没有充分展示。本轮保留产品式版面，补齐以下独立验收：

- [x] 记录相同离散滚动输入下的帧间隔基线；统一 RAF 调度、时间步长平滑和渲染热路径优化。
- [x] 模型旁默认显示带引线的名称与职责；手机提供关联编号和可读图例。
- [x] 模块化章节实际展示接口固定、提供方脱离和替代提供方接入；不暗示任意接口可以混插。
- [x] 执行章节实际展示输入、上下文准备、模型请求、工具调用、工具结果回流、再次请求和最终输出；可播放、暂停、逐步查看。
- [x] 优先复用现有资产；确有缺失时先生成同风格多视角概念图，再用 Blender 建模和导出验证。
- [x] 实际浏览器检查动画状态、标注可读性、手机布局、减少动态效果与案例回归。

本轮复用既有 GLB，无新增建模缺口，未生成新概念图。性能同场景对比、最终集成复测与浏览器覆盖见 `docs/performance/README.md`、`docs/QA.md`。

## 2026-09-19 场景交界连续化

上一轮帧率优化未覆盖模型显隐与布局切换的连续性。本轮将首页所有章节统一到同一场景状态混合：共享语义组件复用同一个模型，章节末段连续插值位置、旋转、尺寸、透明度及标注偏移；仅出现或消失的组件渐入/渐出，流线同步交接。手机镜头按两个端点先各自计算适配尺度，再插值。

- [x] 实际 GLB 基线复现前五个交界的显隐跳变。
- [x] 六个章节交界、过渡起点、正向/反向/任意 seek 连续性自动检查；手机镜头边界检查。
- [x] 保留插件接入、执行循环的播放/暂停/逐步阅读，以及 reduced-motion 静态场景。
- [x] 实际页面滚动和章节跳转、桌面/手机、案例渲染与性能回归；记录验收结果。

## 2026-09-20 产品页视觉统一

参考用户指定的 Apple iPhone Duo 页面，保留既有教学内容与连续模型场景，统一首页、案例库和案例阅读页。

- [x] 记录参考页与现成代码适配/许可结论；统一黑白、字体、间距、圆角与控制栏。
- [x] 首页分离标题、产品与说明的视觉层次，消除跨章文字叠压；案例库改为视觉展陈。
- [x] 案例页从密集三栏改为案例导语、大幅演示与步骤说明，保留所有教学操作。
- [x] 桌面/手机、双语/主题、八案例、键盘与减少动态回归；更新构建和验收记录。

## 2026-09-20 案例模型遮挡修复

- [x] 卡片图片固定在媒体框内，消除图片固有高度撑开网格导致的裁切。
- [x] 案例名称移至独立编号图例，画布保留小编号与选中引线；调整镜头与工具区间距。
- [x] 浏览器检查桌面、手机、英文、主题、图例选择与二维切换；构建及 14 项检查通过。
