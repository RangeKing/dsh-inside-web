# DSH Inside Web

## 产品与读者

本站是独立、双语的 DSH 科普网站：滚动首页介绍组成与执行机制，案例库解释具体任务。读者用过 AI，但未必理解 Harness。教学演示不调用模型或真实工具，不冒充实时 DSH。

网页中的主角统一称“大肥鲸”（英文 Big Whale）；技术资产 ID 可保留 `orca_*`。中文可见文案、替代文本和提示中不使用“虎鲸”。气质为幽默的深海探索与机械剖解；笑点辅助理解，技术语义保持准确。首页视觉以苹果产品介绍页为参考：克制的黑白基底、超大文字、产品级主角光照、全屏停驻场景与连续滚动镜头。避免仪表盘式装饰、重复左右分栏和密集小标签。

## 已确认的体验约束

- 首页用自然滚动驱动模型入场、外壳展开、舱室聚焦与协作，支持反向滚动和章节跳转。跨章节共同组件保持同一模型实例，位置、旋转、尺寸与透明度连续混合；边界检查同时覆盖模型状态和手机镜头，帧率通过不等于场景连续。顶部提供直达案例，底部提供主要案例入口。
- 模型默认显示名称、职责与引线；手机以编号关联图例，点击补充说明。插件动画展示固定接口与兼容提供方替换，执行动画展示输入、工具结果回流、再次请求及最终输出；均可暂停和逐步阅读。
- 点击模块打开简短解释及来源；自由旋转通过独立的探索模式开启，退出后恢复阅读。照顾键盘、触屏与 reduced-motion。
- 案例默认以共享的大肥鲸和功能模块表达步骤；二维关系图与单接口详情提供精确阅读。模型、时间线、文字和选中实体来自同一教学状态。
- 保留八个案例的生活情境与学习目标，按新版 DSH 逐个核验和优化。创造模式改为“把 DSH 变成一家像素风 AI 公司”：安装办公室插件，接到番茄钟网页订单，展示设计、开发、测试、返工和交付。
- 像素风限于公司案例的插件界面，网站其余部分保持 3D 深海视觉。公司成员活动对应教学步骤，不暗示真实 Agent 在后台工作。
- 中英文、深浅主题、独立案例地址、返回阅读位置是完整体验的一部分。默认中文深色。

## 事实、版本与来源

涉及 DSH 内容时先读 `docs/SOURCES.md` 和 `docs/UPSTREAM_AUDIT.md`（建立后），对照锁定提交的官方文档及源码。版本、提交、核验日期放在来源文档和数据元信息中，不在本文件固化版本号。开发开始与交付前检查上游漂移；逐项核验后才更新声明，不能批量替换 commit 冒充核验。

模块空间位置和大肥鲸拟物关系是本站教学设计，不是官方固定分层；“everything is a plugin”包括模型适配与 Agent Loop。预设与启动 Profile 区分。安装、配置保存、运行时生效、实际调用成功分别表达。实验能力标明条件，不当作默认功能。

教学编排、源码支持的行为与导入日志分开标注。未知事件保持未知，不能补画缺失因果。隐藏思维链不作为可观察过程；日志只在本地内存中处理，不上传。

## 资产与工程

复用参考项目 `/Users/rangeking/Code/dsh-inside/assets/` 的现有模型，优先核对 production 清单、元数据和实际 GLB。参考项目只读，不覆盖其已有修改。迁入本站的资产记录源路径、SHA-256、节点、单位、轴向与许可状态；公开分发前解决未确认的外部分发权。

资产路径和节点映射集中管理。业务状态使用语义 ID；渲染层负责 GLB 映射、镜头与动画。首次阅读可先显示文字和模型海报，GLB 异步加载；移动端降低特效但保留内容。加载失败、WebGL 不可用、上下文丢失提供可操作的二维/文字路径。

渲染器管理自己的监听器、RAF、纹理、几何体、材质与加载生命周期。滚动和时间线 seek 直接计算目标状态；滚动目标用帧率无关补间连续追随，静态网格按材质批处理并保留独立活动部件，热路径复用几何缓冲；切换页面不遗留后台渲染。

本站按静态网站构建，不再要求单个离线 HTML。先保留当前原始页面与生成源的可恢复快照，再统一到一个构建入口。不要只改构建产物。新增依赖锁定；本地运行不依赖 CDN。

## 执行与验收

已确认设计、阶段计划与验收矩阵见 `REPOSITORY_PLAN.md`；跨阶段工作开始和结束时更新相应状态。常规实现选择自主处理，不反复请求确认。公开发布和远程推送不在本次授权内。

内容变更验证来源、双语和案例逻辑；渲染变更验证实际 GLB 加载、交互、尺寸、降级与销毁；交付验证桌面/手机、键盘、深浅主题、减少动态效果和所有八个案例。按影响运行检查，发现回归修复后再交付。

最终区分文档完成、代码完成、浏览器验证、真实 DSH 执行和公开上线。静态教学页面测试通过不能表述为真实 DSH 运行验证。
