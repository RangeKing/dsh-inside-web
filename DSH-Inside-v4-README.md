# DSH Inside v4

**把 DeepSeek Harness 的任务执行过程变成可逐步阅读的交互式地图。**

本版在 v3 的案例库、四种 Agent 预设、接口图鉴和运行记录基础上，重点处理四个阅读体验问题：中文表达、步骤连续性、平面视图和焦点层级。

## 本版更新

### 1. 全站中文润色

对案例标题、提示词、步骤标题、运行状态、提示条和模式说明重新做了一轮中文校对。原则是：

- 优先使用常见、完整的书面表达；
- 避免为了“像产品文案”而压缩成生硬短句；
- 技术概念保留官方标识，例如 `run_code`、`ctx.fs`、`cordis_define`；
- “Cordis”作为技术名称保留，面向用户的中文预设名称统一为 **创造模式**。

例如照片案例现在叫 **“整理照片前先确认：用户拒绝后会发生什么？”**，并明确说明：提示词里的确认要求需要真实审批策略配合，拒绝会发生在文件操作执行之前。

### 2. 连续的步骤过渡

任务导览在步骤切换时保留上一阶段的视觉状态约 900 ms：

- 上一批高亮组件逐渐淡出；
- 当前组件逐渐亮起；
- 上一步的路径短暂保留；
- 当上一阶段终点和当前阶段起点不同，会出现一条短暂的过渡连接；
- 当前数据包沿新的方向继续流动。

因此手动切换步骤和自动播放都能看见“任务从哪里流向哪里”，减少瞬间换图带来的跳跃感。

### 3. 新增平面模式

任务导览右上角可在 **3D / 平面 / 剖面** 三种视图之间切换。

- **3D**：保留六个概念层的空间关系，适合建立整体结构感；
- **平面**：使用固定方向的六条能力带，节点位置稳定，更适合逐步跟随任务；
- **剖面**：只查看一个服务的声明包、直接消费者和实现方。

快捷键 `P` 可切换 3D / 平面；`F` 进入或退出剖面。

### 4. 更强的焦点淡化

当前步骤之外的节点、顶面图标、层板和标签都进一步降低了亮度。高亮范围由当前步骤的数据决定，切换阶段时再平滑交接给下一批节点。

### 5. 四种预设名称

页面继续展示官方固定提交中的四个 shipped Agent presets：

- `minimal` → **Minimal / 极简**
- `standard` → **Standard / 标准**
- `ptc` → **PTC / 程序化**
- `cordis` → **创造模式**

“创造模式”是网页面向中文用户的名称，源码路径、工具名和运行时标识仍使用官方 `cordis` / `cordis_*` 命名。

## 案例库

八个案例按难度排列：

1. DSH 是怎么完成一项任务的？
2. 三人聚餐后，怎样算清转账？
3. 整理照片前先确认：用户拒绝后会发生什么？
4. 修改共享采购单时，怎样保留家人的更新？
5. 合并三张报名表，找出重复报名的人
6. 迎新活动能否按计划举办？让两个子 Agent 分别核对
7. 从 1200 份航行档案中找出三条关键线索
8. 创造模式：临时做一个分账工具

每个案例都说明“这个案例展示哪些 DSH 特点”，并提供教学资料、逐步状态和最终结果。

## 打开网页

根目录的 `index.html` 是构建后的独立网页。直接用现代桌面浏览器打开即可，无需模型 Key、Node.js 或 DSH 后端。

如需通过本地 HTTP 访问：

```sh
python -m http.server 8080
```

## 重新构建

只修改 `src/` 或当前 `data/` 后：

```sh
python scripts/build.py
```

需要从 v3 的案例/模式生成脚本重新生成数据时，再应用 v4 内容校对：

```sh
python scripts/make_cases_v3.py
python scripts/make_modes_v3.py
python scripts/patch_v4_wording.py
python scripts/polish_v4.py
python scripts/build.py
```

## 数据与源码依据

- 上游：`deepseek-ai/deepseek-harness`
- 固定提交：`d347e703908d0406b7a7ef80e3a0e594d86b2215`
- DSH 版本：`0.1.3-alpha.1`
- 服务目录：70 个服务（29 seam / 40 core / 1 bundle）

内置案例是依据源码行为编排的教学演示，没有执行真实 DSH、没有调用模型，也不能用于性能测量。

## 测试

本版新增的 v4 验收项包括：中文标题、创造模式名称、3D/平面/剖面互斥关系、步骤连续过渡、非焦点节点强度与双语回归。

本次交付通过：

- v4 专项检查：18 / 18
- v3 回归 A：77 / 77
- v3 回归 B：31 / 31
- v3 回归 C：39 / 39
- v3 回归 D：34 / 34

测试环境使用 Chromium；当前自动化主要覆盖 Canvas 软件三维后端。Safari、Firefox、硬件 WebGL 和真实 DSH 后端连接仍未纳入本次验收。

## License

页面代码使用 MIT License。DeepSeek Harness 的资料和少量源码片段遵循上游 MIT License；详见 `THIRD_PARTY_NOTICES.md`。
